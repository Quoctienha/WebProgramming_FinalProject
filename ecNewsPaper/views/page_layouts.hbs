<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
</head>
<body>
  <div class="container">
    <h1>{{title}}</h1>
    <p>{{content}}</p>
    <a href="/" class="btn btn-primary">Go Back</a>

    <ul>
      {{#if posts.length}}
        {{#each posts}}
            <a href="/post/{{PostID}}">{{PostTitle}}</a>
            <br>
        {{/each}}
      {{else}}
        <li>No posts found for this subcategory.</li>
      {{/if}}
    </ul>
  </div>
</body>
</html>
